# Neural_Network
Implementation of a neural network from scratch.

## Summary
Neural network is optimised with an adam optimiser and has l2 regularisation to prevent overfitting. Dataset is split into test and train subsets to show that the model is not overfit.
